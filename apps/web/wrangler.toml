# Cloudflare Pages (Git CI): Pages forbids `main` / `[assets]` alongside `pages_build_output_dir`.
# Use wrangler.worker.toml for Worker + static assets deploy (`wrangler deploy --config wrangler.worker.toml`).
name = "gold-web"
pages_build_output_dir = "./dist"
compatibility_date = "2026-03-30"
